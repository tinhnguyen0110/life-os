"""modules/projects — the Projects feature module (Sprint 1, Tier-S).

Locks the common ProjectStatus shape every screen + later reader inherits.
The registry discovers MODULE from router.py (T2). This package exposes the
schema / reader / service that the router and wiring tasks build on.
"""

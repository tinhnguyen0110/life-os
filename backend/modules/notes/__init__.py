"""modules/notes — Notes feature module (Sprint 6, SPEC §S9-area).

Markdown notes under md_store `notes/<id>.md` (YAML front-matter + body); every
write is one git commit. Substring search, attach to a project/channel/none.
The registry discovers MODULE from router.py (T2).
"""

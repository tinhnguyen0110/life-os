# End Sprint FINANCE-AUDIT-S3 — held-coin OHLC capture (the tower LIGHTS UP from real holdings)

> Status: **REVIEWED — 3 gates green, committing.** Task #62. The "fix works, data thin" follow-on the S2/S1B end_sprints flagged. S2 made s_asset read held coins; S3 FEEDS them real price-history → RSI computes → s_asset escapes 0 → the full finance-assistant tower is alive end-to-end, lit from the user's REAL positions. Capability add, NEUTRAL.

## What shipped (the data deepens)
- **`_fetch_okx_candles(symbol, *, limit)` (market/service.py)** — an UNSIGNED public GET to OKX `/api/v5/market/candles?instId=<SYM>-USDT&bar=1D` (a plain `_httpx.get`, NOT `exchange.reader._get` which signs every request — held coins are tradeable regardless of the user's account). **LOCK #2:** OKX candle = `[ts_ms, open, high, low, CLOSE(idx4), vol, ...]` returned NEWEST-first → takes `close = float(r[4])`, converts ts ms→ISO UTC, and `sort()`s to ASCENDING (oldest→newest, what `history()`+RSI expect). Fail-open → `[]` (caller falls back / honest-skips). Drops `close <= 0`.
- **`capture_held_history(*, days=30, limit=100)` (market/service.py)** — reads `decision._held_symbols()` (dust+stablecoins excluded). Per held coin: OKX public candles PRIMARY → `reader.fetch_market_chart(sym.lower(), days)` CoinGecko FALLBACK → honest-skip if NEITHER (NEVER fabricate). Writes via `db.record_price(sym, close, ts_iso, source="okx-candles"/"coingecko-hist")` (REUSE — no parallel store). **LOCK #1 idempotent:** dedup by (asset, UTC day) via `db.price_days(sym)` — a day already present is skipped, AND days are added to the local set within the fetch loop (guards same-fetch dups). Re-run / daily-overlap / backfill-then-daily → row count STABLE. Fail-soft PER coin (one coin's fetch raising → it errors in the summary, the others still capture). Returns `{sym: {inserted, skipped, source, error?}}`, honest source tag.
- **Daily routine + the backfill-in-one (market/router.py + automation/service.py)** — `_HELD_HISTORY_ROUTINE` (id `held-history`, cron 00:10 UTC after the day rolls), wired into `MODULE.routines` (registry — NO core/main edit). `_held_history_work()` runs the capture, gated on the master automation switch + records a `run_log` row (mirrors market-poll). The automation `_CATALOG` + `_external_func` resolve the market-owned func on demand (same pattern as macro-snapshot). **No separate backfill path:** `capture_held_history(days=30, limit=100)` fetches 100 candles + inserts the last 30 on the FIRST run — so the first daily run IS the ~30d backfill (idempotent, so re-runs add only genuine gaps). One function, one dedup discipline, serves both — cleaner than a separate backfill trigger.
- **s_asset lights up — NO code change** to `_s_asset`/`compute_indicators`/`history`/`record_price`/`decision_weight` (the S2 read path + the W=0 valve are byte-unchanged). S3 only feeds data; once a held coin has ~14+ rows, `compute_indicators` → real RSI → `_asset_signal` present → s_asset > 0.

### Verified counts (architect re-ran independently — Rule #0)
- decision + market + market_api + automation + automation_api: **231 passed, 0 errors, 0 failed** (107.78s; the only warning = the benign starlette/httpx-testclient deprecation).
- Full suite: **1630 passed, 6 skipped, 0 failed, 0 errors** (was 1623; +7 S3 tests), 1 benign starlette/httpx deprecation warning.
- mypy: the 3 S3-touched files (`market/service.py`, `market/router.py`, `automation/service.py`) **clean** (the 3 mypy errors are pre-existing in `claude_usage/service.py` + `exchange/service.py` — untouched by S3).
- **team-lead LIVE-verified on the container (Rule #0): THE TOWER IS LIT** — all 6 held coins (PEPE/ICP/ARB/S/TRUMP/IP) got **30 price_history rows each, source `okx-candles`**; **PEPE RSI = 42.87 REAL** (was None); **s_asset 0.0 → 0.1836** (6/6 held now have technicals); **decision_weight W 0.0 → 0.0239**, verdict "thin", binding s_asset, ALL 4 layers non-zero (q_cycle 0.514 · q_macro 0.437 · q_flow 0.579 · s_asset 0.184).

## ⚠️ THE TOWER LIT FROM REAL HOLDINGS (for the user — the headline)
Live `W` went **0.0 → 0.0239** — NOT from a hardwire or a fabricated series, but because the 6 held coins now carry **real OKX daily closes** (30 rows each) → real RSI → s_asset 0→0.184. W is still "thin" (0.0239) because that's the HONEST product of four modest-confidence layers — the brake works: a high product would need genuinely strong+fresh signal on all four. **The full finance-assistant decision tower (data→state→weight→policy) is now alive end-to-end, lit from the user's REAL positions.** As price-history deepens daily (the routine), the technical signal sharpens — no further code.

## Code review (architect — 4-step, the 5 team-lead points + the distinguishing spine hardest)
1. **git status/diff** — files STABLE (mtimes ~12:25–12:26, reviewed 12:43 — >15min, not in-flight; backend-2 silent-after-done pattern → stability-checked). 6 files: market/service (the fetch+capture), market/router (the routine), automation/service (catalog+resolver), test_decision (+143 lines S3 tests), test_automation + test_automation_api (routine-count 7→8). `template/*` (pre-existing dirty, NOT S3) + `data/` EXCLUDED.
2. **Read full functions** — `_fetch_okx_candles` (unsigned `_httpx.get`, close=idx4, ms→ISO, sort ascending, fail-open []); `capture_held_history` (held_symbols → OKX→CG→honest-skip, price_days dedup + in-loop set guard, fail-soft per coin); `_held_history_work`/`held_history`/`_HELD_HISTORY_ROUTINE` (gated, run_log, registry-wired); `_external_func` held-history branch.
3. **Verify against plan + the 6 acceptance gates** — real rows/honest-source (G1), RSI+s_asset lights (G2), distinguishing valve (G3), idempotent (G4), close/ascending/sanity parse (G5), fail-soft (G6). All have a dedicated test.
4. **Hunt additional issues — the 5 team-lead review points, each verified in code:**
   - **(1) unsigned OKX fetch** — `_httpx.get` (module-level alias L684), NOT `exchange.reader._get`; close=`float(r[4])` (idx 4); `out.sort()` ascending (OKX newest-first); ms→ISO UTC. ✅
   - **(2) idempotent dedup by (asset,day) via `price_days`** — caller skips a present day + adds to the local set in-loop; GATE4 `run-twice → 30→30 stable`. Covers backfill+daily overlap (one fn, one dedup). ✅
   - **(3) DISTINGUISHING spine** — GATE3: `HASOKX` (real series → `_asset_signal > 0`) AND `NOSRC` (no source → `None`), SAME `_s_asset()` call → `"1/2" in note`. Genuinely divergent; no fabricated backfill forces the tower on; the valve holds for the no-source coin. ✅
   - **(4) read path / W=0 valve UNCHANGED** — no edit to `_s_asset`/`compute_indicators`/`history`/`decision_weight`; the GATE2 pure-product + zero-blinds (W=0) tests still pass alongside. S3 only feeds data. ✅
   - **(5) the parse unit test uses MONKEYPATCH** — `monkeypatch.setattr(ms._httpx, "get", ...)` (auto-restores) with an explicit comment that a bare `ms._httpx.get = ...` would LEAK into later tests (the httpx-shared-module poison backend caught + fixed). Verified it is monkeypatched, not a raw assignment. ✅

## Assumptions (user-review)
- **okx-public-unsigned-candles** — held-coin daily OHLC comes from OKX `/api/v5/market/candles` (PUBLIC, no-auth, a plain unsigned GET — held coins are tradeable regardless of the account). CoinGecko `fetch_market_chart` is the fallback; honest-skip if neither. **Why:** held coins aren't in `tracked_assets` so the CoinGecko-tracked-only `backfill` never reached them → no RSI-able series → s_asset stuck 0. **How to change:** `_fetch_okx_candles` / the fallback order in `capture_held_history`.
- **held-history daily routine (00:10 UTC) + 30d-in-first-run backfill** — `capture_held_history(days=30, limit=100)` fetches 100 candles and inserts the last 30; the FIRST daily run IS the ~30d backfill (RSI needs ~14+). **Why:** one idempotent function serves both backfill + daily, no separate trigger. **How to change:** the routine `trigger_args` (cadence) / the `days`/`limit` window.
- **idempotent-by-(asset,day)** — dedup via `db.price_days` (same discipline as `backfill`/nav/macro-snapshot's day-key upsert); one row per asset per UTC day, re-run-safe. **Why:** LOCK #1 — backfill + daily must not double-insert. **How to change:** the dedup key in `capture_held_history`.
- **s_asset now lights from real holdings** — no code change to the S2 read path; held coins gaining ~14+ real rows → real RSI → s_asset > 0 → W escapes 0. The W=0 valve is intact (a no-source coin still contributes 0; the GATE3 distinguishing proves real-per-coin, not blanket-fake). **Why:** the audit spine — "sửa đường ống, không tháo phanh" (fix the pipe, keep the brake). **How to change:** N/A — feeding data, the derivation is unchanged.

## The 3 Quality Gates
- **Gate 1 — API:** ☑ `/routines` shape unchanged (total 7→8, held-history added; activeCount math consistent) · ☑ module registry-wired (NO core/main edit) · ☑ no auth · ☑ NEUTRAL (capturing OHLC is data, no buy/sell) · ☑ fail-open (no data → honest-skip, no crash). **PASS**
- **Gate 2 — Function:** ☑ (1) real rows/honest-source · ☑ (2) RSI+s_asset lights end-to-end · ☑ (3) DISTINGUISHING valve (real-per-coin, no-source→0, SAME call) · ☑ (4) idempotent run-twice-stable · ☑ (5) close/ascending/sanity parse · ☑ (6) fail-soft per coin · ☑ read path + W=0 valve unchanged (GATE2 product/zero-blinds pass) · ☑ unsigned fetch (not signed _get) · ☑ monkeypatch (no httpx leak) · ☑ existing tests pass · ☑ **0 errors** · ☑ S3 files mypy clean. **PASS**
- **Gate 3 — Sprint:** ☑ end doc w/ verified counts + the tower-lit live-state · ☑ architect spot-checked full functions (5 review points in code) · ☑ counts ≥ baseline · ☑ team-lead LIVE-verified (W 0→0.0239, 6/6 RSI) · ☑ assumptions logged (4) · ☑ commit format `feat(sprint-FINANCE-AUDIT-S3)`. **PASS**

## Risks / follow-ups
- **The finance-assistant tower is now alive end-to-end** — data (OKX/CG/FRED) → state (s_asset/macro_cycle/q_flow) → weight (W, the ∏ with the brake) → policy (allocation/guardian, NEUTRAL). S1/S1B made confidence honest (cadence, mock-excluded); S2 read real holdings; S3 fed them real history. The audit spine held: every fix corrected the MEASURE or FED real data — none removed the brake.
- W stays "thin" (0.0239) — HONEST, the product of four modest layers; it sharpens as price-history deepens daily (the routine), no code. A held coin on NEITHER source stays honest-0 (the valve).
- Process: backend-2 silent-after-done (5th occurrence) — verified solid 3 ways (disk re-run 231-green + team-lead live W-0→0.0239 + this 5-point code review). The op-model silent-report gotcha persists; the stability-check (mtime >2min before review) is the standing guard.
